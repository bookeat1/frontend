/** 4pt base spacing scale. Never hardcode raw numbers in screens — use these. */
export const spacing = {
  none: 0,
  xxs: 2,
  xs: 4,
  sm: 8,
  md: 12,
  lg: 16,
  xl: 20,
  xxl: 24,
  xxxl: 32,
  huge: 40,
} as const;

/**
 * Corner radii — verified against Figma node 340:2535 and sibling screens.
 * Named by the role they play, not by their pixel value.
 */
export const radius = {
  /** Chips, pill buttons, icon buttons, the search input. */
  pill: 999,
  /**
   * Плавающая плашка нижней навигации (Figma 3z0f6dgev4HMwBAHPjTjPo,
   * node 3039:23943 — `rounded-[36px]`). Отдельный токен, а не `pill`:
   * в макете это конкретные 36. При высоте плашки 58 RN всё равно обрежет
   * радиус до половины высоты (29), поэтому визуально это капсула — но
   * значение остаётся тем, что нарисовано.
   */
  navBar: 36,
  /** Cards: menu item photos, promo banners, map preview, search result photo. */
  card: 20,
  /** The single largest photo on screen — restaurant cover photo only. */
  photoHero: 24,
  /** Rounded-square venue avatar at the top of the Reservation detail screen
   * (node 488:9876) — a squircle, not a circle. */
  avatar: 18,
  /** Confirmation dialog sheet. */
  dialog: 24,
  /**
   * Верхние углы НИЖНЕЙ ШТОРКИ «Дата и гости» на главной — 16 (Figma
   * 3z0f6dgev4HMwBAHPjTjPo, node 3447:13024 — `rounded-t-[16px]`). Отдельный
   * токен, а не `dialog` (24): диалог подтверждения и шторка подбора — разные
   * вещи, и в макете у них разный радиус.
   */
  sheet: 16,
  /**
   * Карточка брони в списке «Мои брони» (Figma dVjT37j984ErvOmzxlx29p,
   * node 3004:6807 — `rounded-[24px]`). Отдельный токен, а не `dialog`:
   * значение совпадает, но роль другая, и диалог не должен тянуть за собой
   * карточку списка, если один из двух радиусов поменяют.
   */
  bookingCard: 24,
  /**
   * 16 — промежуточный радиус.
   *
   * Фотографии карточек главной сюда БОЛЬШЕ НЕ ходят: в макете
   * (3z0f6dgev4HMwBAHPjTjPo, узлы 3447:12749, 3447:12869 и соседние) у них
   * `rounded-[20px]`, то есть `card`. Прежние 16 были сняты с отрендеренного
   * экрана `design-ref/screen-explore.png` на глаз, а не с узла.
   *
   * Остаётся как основа для мелких скруглений (квадрат чекбокса в фильтрах —
   * `media / 3`).
   */
  media: 16,
  /**
   * Белый блок содержимого на сером листе экрана «Статьи»
   * (Figma dVjT37j984ErvOmzxlx29p, node 1001:11921 — `rounded-[24px]` у блока
   * заведения, `rounded-b-[24px]` у шапки статьи). Отдельный токен, а не
   * `dialog`/`photoHero`: значение совпадает, но роль другая — это лист
   * содержимого, и правка радиуса диалога не должна тянуть за собой статью.
   */
  contentBlock: 24,
  /**
   * Белый блок раздела главной («Выбрали для вас», «Афиша», «Акции») — 24
   * (Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3102:12006, 3102:12024, 3102:12039,
   * 3228:9819: у всех `cornerRadius: 24`). Отдельный токен, а не `card` (20):
   * блок раздела — это лист, на котором лежат карточки, и радиус самой
   * карточки не должен тянуть его за собой.
   */
  homeSection: 24,
  /**
   * Карточка ВЕРТИКАЛЬНОГО СПИСКА нового вида — 22 (Figma
   * 3z0f6dgev4HMwBAHPjTjPo, node 3452:13344 «Card / Flour Demi»:
   * `rounded-[22px]`). Отдельный токен, а не `card` (20): у новой карточки
   * скругляется вся карточка целиком, а не фотография внутри неё, и в макете
   * это конкретные 22.
   */
  listCard: 22,
  /**
   * Нижние углы шапки карточки заведения — 28 (node 3446:12622: кадр
   * `Ocean Basket Hero` 375x350 со скруглением 28). Больше `photoHero` (24):
   * прежняя обложка отступала от краёв экрана на 8, новая идёт от края до
   * края, и при той же оптике скругление должно быть крупнее.
   */
  hero: 28,
  /**
   * Поле ввода даты рождения на регистрации (Figma
   * 3z0f6dgev4HMwBAHPjTjPo, node 3073:11656 — `rounded-[16px]`). Отдельный
   * токен, а не `media`: значение совпадает, но роль другая.
   */
  field: 16,
  /**
   * Карточка списка «Афиша» — 22 (Figma 3z0f6dgev4HMwBAHPjTjPo, node
   * 3452:13198 «Card / Flour Demi» — `rounded-[22px]`). Своё значение, а не
   * `card` (20) и не `photoHero` (24): в макете этой карточки нарисованы
   * именно 22, и она единственная в приложении с таким радиусом.
   */
  eventCard: 22,
  /**
   * Нижние углы шапки гастрогида — 28 (Figma 3z0f6dgev4HMwBAHPjTjPo,
   * node 3192:6247, `rounded-b-[28px]`). Отдельный токен, а не `contentBlock`
   * (24): в макете это именно 28, и шапка — единственное место с этим
   * радиусом.
   */
  guideHero: 28,
  /**
   * Плитка рубрики в ряду гастрогида — 22 (node 3192:6097). Между `card` (20)
   * и `contentBlock` (24), и это не описка макета: у всех четырёх плиток
   * `rounded-[22px]`.
   */
  guideRubric: 22,
  /**
   * Большая карточка гастрогида — «Выбор редакции» и «Гастропрогулка»
   * (узлы 3192:6266, 3192:6275, 3192:6279) — 24. Отдельный токен, а не
   * `contentBlock`: значение совпадает, но роль другая. Здесь это САМА
   * карточка с фотографией, а не белый лист под карточками, и правка радиуса
   * листа не должна её тянуть.
   */
  guideCard: 24,
  /** Карточки страницы бренда (узлы 3441:12289, 3443:12468, 3443:12573) — 16. */
  brandCard: 16,
  /** Карта и блок брони на странице бренда (узлы 3426:9633, 3443:12583) — 22. */
  brandBlock: 22,
  /** Пилюля-рубрика в шапке страницы бренда (node 3425:3935) — 19. */
  brandPill: 19,
} as const;

/** Minimum hit-slop / touch target per accessibility hard rule (>= 44pt). */
export const hitSlop = {
  minTouchTarget: 44,
} as const;

/**
 * Fixed control heights the design states explicitly. Verified against the
 * Reservation screen (Figma file oPxXynSOY3PYhf3gkVR5Ps, node 471:3880).
 */
export const controlHeight = {
  /** Date/guests pill selectors (471:3899), time slots (471:3914) and the
   * sticky flow CTA (471:3967) are all 48 tall. */
  pill: 48,
  /**
   * Чип-фильтр под строкой поиска — 40 (Figma 3z0f6dgev4HMwBAHPjTjPo,
   * node 347:5942, кадры «Frame 45»/«Frame 46»: высота 40, скругление 999,
   * подложка #F3F2F2).
   *
   * Меньше жёсткого минимума в 44, и это НЕ послабление правила: чип лежит в
   * горизонтальной прокрутке, и до 44 его зона касания добирается hitSlop'ом
   * (см. `FilterChip`) — ровно так же, как у 28-точечных пилюль карточек
   * Explore (`compactPill`). Рисуем то, что нарисовано, пальцем попадаем в 48.
   */
  chip: 40,
  /** "Special Requests" textarea (471:3946). */
  multilineField: 80,
  /** Dish photo in the pre-order list. Measured off the design render
   * `design-ref/screen-menu-preorder.png` (120x80) — that screen has no
   * value-by-value spec yet, so this is a read of the render, not a node. */
  dishPhotoHeight: 80,
  dishPhotoWidth: 120,
  /** Venue avatar on the Reservation detail screen (node 488:9876) — 72x72
   * rounded square, optically centred under the header. */
  venueAvatar: 72,
  /** Circular contact icon button (website / WhatsApp / Instagram). */
  contactIcon: 48,
  /** Map preview block on the Reservation detail screen. */
  mapPreview: 208,
  /** Status pill height — 32 tall with 12/6 padding in the design. */
  statusPill: 32,
  /**
   * Квадратная фотография заведения в строке «Мои брони»
   * (node 3004:6810) — 64x64 со скруглением `radius.card` (20).
   */
  bookingVenueThumb: 64,
  /** Explore card pills: the red time-slot pill on a restaurant card and the
   * grey tag chip on an event card are both 28 tall
   * (`design-ref/screen-explore.png`). They are inside a horizontally
   * scrolling card, so the 44pt rule is met with hitSlop, not with height. */
  compactPill: 28,
} as const;

/**
 * Fixed block sizes of the Explore (home) screen, measured on
 * `design-ref/screen-explore.png` at 1:1 (the render's device frame is exactly
 * 375 wide). No value-by-value Figma spec exists for this screen yet, so these
 * are reads of the render.
 */
export const exploreLayout = {
  /** Full-bleed hero carousel. 245 of it is visible; the last 20 sit behind
   * the white sheet, which overlaps them with its rounded top corners. */
  heroHeight: 265,
  sheetOverlap: 20,
  /**
   * Карточка горизонтального ряда на главной — 256 в ширину, фотография 148 в
   * высоту (Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3447:12749, 3447:12830,
   * 3447:12913 и соседние: `w-[256px]`, `h-[148px]`, просвет между карточками
   * 8).
   *
   * Было 200x120 — значение, выбранное НАМИ на глаз, чтобы в ряд помещалось
   * две целых карточки и третья с краю. Макет рисует одну целую и заметный
   * край второй, и по правилу владельца от 26.08.2026 («значения UI берём из
   * Figma, не подгоняем на глаз») ряд возвращён к макету. Если две карточки в
   * кадре всё-таки нужны — это решение владельца по бете, и менять его надо
   * вместе с этим комментарием, а не молча.
   */
  cardWidth: 256,
  cardPhotoHeight: 148,
  /**
   * Круг с фотографией кухни в ряду «Выберите кухню».
   *
   * 96 — размер из макета (3z0f6dgev4HMwBAHPjTjPo, ячейки 3106:12348 и
   * соседние). Прежние 72 были сняты с отрендеренного экрана, а не с макета,
   * и держались только потому, что так помещалось ~4 кружка вместо ~3.
   * Владелец выбрал макет (2026-08-26): кухню выбирают по картинке, и её
   * читаемость дороже лишней половины кружка в ряду.
   */
  cuisineChip: 96,
  /**
   * Ширина ЯЧЕЙКИ ряда кухонь — она же ширина подписи под кругом. Круг
   * теперь тоже 96, то есть ячейка и круг совпали по ширине.
   *
   * Шире круга нарочно (правка владельца 2026-08-24: «Ср.морская»). Подпись
   * «Средиземноморская» — одно слово, переносить его не по чему, и в 72 оно
   * не влезало ни в одну строку: RN обрезал его многоточием. Сжатие шрифта
   * (`adjustsFontSizeToFit` в `CuisineChip`) даёт самому длинному названию
   * боевого каталога поместиться ЦЕЛИКОМ. Цена — на экране 360 видно ~3
   * кружка вместо ~3.9; обрезанное название стоит дороже.
   *
   * 96, а не прежние 84 (2026-08-26): столько в макете
   * (3z0f6dgev4HMwBAHPjTjPo, ячейки 3106:12348 и соседи), и подпись выросла
   * с 12/16 до нарисованных 14/20 — в 84 длинное название сжималось бы
   * сильнее прежнего.
   */
  cuisineChipLabel: 96,
  /**
   * Фотография события в строке «Афиши» на главной — 110x104 со скруглением
   * `radius.card` (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3228:9840). Не квадрат:
   * в макете кадр шире, чем выше, и именно его высота задаёт высоту всей
   * строки (текст слева ниже и центрируется по ней).
   */
  eventThumbWidth: 110,
  eventThumbHeight: 104,
  /**
   * Высота СОДЕРЖАТЕЛЬНОЙ части шапки главной — того, что лежит НИЖЕ верхней
   * безопасной зоны устройства (Figma 3z0f6dgev4HMwBAHPjTjPo, node
   * 3102:11986: вся рамка шапки 308 при ширине 375, из них 44 занимает
   * статус-бар с часами и батареей → 308 − 44 = 264).
   *
   * Полная высота блока считается как «верхняя вставка + это число» —
   * см. `homeHeaderHeight()` в
   * `apps/mobile/src/components/explore/home-header-layout.ts`. Хранить тут
   * 308 было бы неверно: 44 — это статус-бар КОНКРЕТНОГО макетного
   * устройства, а на реальных он от 20 до 62.
   */
  headerContentHeight: 264,
} as const;

/**
 * Карточка ВЕРТИКАЛЬНОГО СПИСКА — экраны поиска, «Афиша», «Статьи»
 * (гастрогид), «Гастропрогулки». Одна цифра на все четыре: человек ходит
 * между этими списками по одной навигации, и разная высота обложки читалась
 * бы как разные виды карточек, а не как разный материал.
 */
export const listCard = {
  /**
   * Высота карточки — 198 (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3452:13345 и
   * соседи: кадр `Image` карточки «Card / Flour Demi» — `h-[198px]`).
   *
   * Было 206 (узел 3192:6246 гастрогида), до того 148. Вместе с высотой
   * поменялось устройство карточки: название теперь лежит ВНУТРИ снимка, а не
   * под ним, поэтому «высота обложки» стала высотой всей карточки.
   *
   * Высота ФИКСИРОВАННАЯ, а не пропорция от ширины: в макете нарисована
   * конкретная высота, и на экране 360 пропорция дала бы 190, а на 430 — 227,
   * то есть ни на одном реальном телефоне не было бы значения из макета.
   */
  coverHeight: 198,
  /**
   * Внутренние поля подписи на снимке — 18 слева/справа/снизу (node
   * 3452:13345: `pb-[18px] px-[18px]`). Не из шкалы 4pt намеренно: это
   * значение макета, и подгонять его к 16 или 20 значило бы сдвинуть имя
   * места относительно фотографии.
   */
  contentPadding: 18,
  /**
   * Кнопка избранного на снимке — круг 32 в 16 от верхнего и правого краёв
   * карточки (node 3452:13346: `left-[295px] top-[16px]`, высота 32 при
   * ширине карточки 343 → правый отступ 343 − 295 − 32 = 16).
   */
  overlayButtonSize: 32,
  overlayButtonInset: 16,
  /**
   * Просвет между карточками в ленте и боковой отступ самой ленты — по 16
   * (node 3452:13343: `gap-[16px] px-[16px]`). Отступ теперь принадлежит
   * ЛЕНТЕ, а не карточке: снимок больше не отступает от края отдельно от
   * подписи, потому что подпись лежит на снимке.
   */
  gap: 16,
  listPadding: 16,
} as const;

/**
 * Карточка списка «Афиша» — Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3452:13198 /
 * 3452:13199.
 *
 * ОТДЕЛЬНЫЙ токен, а не `listCard.coverHeight` (206), НАРОЧНО: в новом макете
 * афиша перестала быть «обложка сверху, текст под ней». Это кадр с подписью
 * ПОВЕРХ фотографии — другой вид карточки, и её высота (198) не имеет
 * отношения к высоте обложек поиска, статей и гастропрогулок. Приравнять их
 * значило бы, что правка одной молча поедет в три чужих списка.
 */
export const eventListCard = {
  /** Высота кадра — 198 (node 3452:13199, `h-[198px]`). */
  coverHeight: 198,
  /**
   * Поля подписи внутри кадра — 18 по бокам и снизу (node 3452:13199,
   * `pb-[18px] px-[18px]`). В шкале `spacing` шага 18 нет и быть не должно:
   * это внутреннее поле одной карточки, а не общий отступ раскладки.
   */
  contentPadding: 18,
  /** Просвет между названием и подписью — 2 (node 3452:13200). */
  titleGap: 2,
} as const;

/**
 * Шапка карточки афиши — Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3452:13224…13250.
 * Фотография во всю ширину, поверх неё градиент, плавающие кнопки и подпись.
 */
export const eventHero = {
  /** Высота фотографии — 350 (node 3452:13270). */
  photoHeight: 350,
  /** Подпись отступает от левого края кадра на 24 (node 3452:13241, `left-[24px]`). */
  contentInset: 24,
  /**
   * Подпись отстоит от НИЗА кадра на 22: блок начинается на 268 от верха
   * фотографии... точнее, на 268−44 = 224 от её верха, его высота 104
   * (38 название + 4 + 20 подпись + 12 + 30 метки), а сама фотография 350 →
   * 350 − 224 − 104 = 22.
   */
  contentBottom: 22,
  /** Просвет между блоком «название + подпись» и рядом меток — 12 (node 3452:13242). */
  contentGap: 12,
  /** Просвет между названием и подписью — 4 (node 3452:13243). */
  titleGap: 4,
  /** Плавающая кнопка — 40x40 со скруглением 20 (узлы 3452:13229, 3452:13234). */
  controlSize: 40,
  /**
   * Кнопки лежат на 14 ниже безопасной зоны: в макете `top-[58px]` при
   * статус-баре 44 → 58 − 44 = 14. Хранить 58 нельзя — 44 это статус-бар
   * КОНКРЕТНОГО макетного устройства.
   */
  controlTop: 14,
  /** Отступ ряда кнопок от боков — 16 (node 3452:13228, `left-[16px]`, ширина 343). */
  controlInset: 16,
  /** Просвет между сердечком и «поделиться» — 8 (node 3452:13233). */
  controlGap: 8,
  /**
   * Метка-пилюля на кадре (node 3452:13249): поля 12 по горизонтали и 7 по
   * вертикали, просвет между пилюлями 6. Ни 7, ни 6 в шкале `spacing` нет —
   * это внутренние размеры одной пилюли.
   */
  pillPaddingH: 12,
  pillPaddingV: 7,
  pillGap: 6,
} as const;

/**
 * Раскладка гастрогида «Editorial v2» (Figma 3z0f6dgev4HMwBAHPjTjPo,
 * node 3192:6246). Числа, которых нет в шаге 4 pt (14, 22, 28, 118, 158, 206,
 * 214) — они нарисованы, и подгонять их к шкале значило бы рисовать не то,
 * что в макете.
 *
 * ПРО «20 ПРОТИВ 24» И ОТСТУП ФОТОГРАФИИ 8. Оба вопроса закрыты новым
 * макетом: скругление карточек гастрогида теперь 24 (`radius.guideCard`),
 * плиток рубрик — 22 (`radius.guideRubric`), а бокового отступа фотографии
 * внутри карточки больше нет вовсе — карточка занимает всю ширину контента,
 * а поля 16 стоят у листа целиком (`contentPaddingHorizontal`).
 */
export const guideLayout = {
  /**
   * Высота СОДЕРЖАТЕЛЬНОЙ части шапки — того, что ниже верхней безопасной
   * зоны (node 3192:6247: кадр 340 при статус-баре 44 → 340 − 44 = 296).
   * Хранить 340 нельзя: 44 — статус-бар макетного устройства, на реальных он
   * от 20 до 62.
   */
  heroContentHeight: 296,
  /** Блок слогана стоит на 24 от нижней кромки кадра (node 3192:6253:
   * y 223 + высота 93 = 316 при кадре 340). */
  heroCopyBottom: 24,
  /** Просвет между надписью-рубрикой, слоганом и строкой под ним — 6. */
  heroCopyGap: 6,
  /** Верхнее поле листа контента — 24, нижнее — 28 (node 3192:6257). */
  contentPaddingTop: 24,
  contentPaddingBottom: 28,
  /** Боковые поля листа контента — 16 (node 3192:6257). */
  contentPaddingHorizontal: 16,
  /** Просвет между секциями — 28 (node 3192:6257). */
  sectionGap: 28,
  /** Просвет между заголовком секции и её содержимым — 12 (узлы 3192:6258:
   * лента на 44 при шапке 32; 3192:6263 и 3192:6271: содержимое на 40 при
   * шапке 28). БЫЛО 14 — считалось по прежней шапке в 24 pt Playfair. */
  sectionHeaderGap: 12,
  /** Просвет между двумя большими карточками в секции — тоже 12 (node
   * 3192:6271: карточка 3192:6275 стоит на 40, следующая 3192:6279 на 258
   * при высоте 206 → 258 − 40 − 206 = 12). БЫЛО 14. Значение то же, что у
   * `sectionHeaderGap`, но роль другая: правка одного не должна двигать
   * второе. */
  cardGap: 12,
  /**
   * Высота строки заголовка секции — 28 (узлы 3192:6264 и 3192:6272), она же
   * межстрочный интервал заголовка. БЫЛО 32.
   *
   * МАКЕТ ЗДЕСЬ НЕ СХОДИТСЯ САМ С СОБОЙ: у «Рубрик» (node 3192:6259) строка
   * заголовка проставлена руками в 32, у «Выбора редакции» и «Гастропрогулок»
   * она автоматическая и равна 28. Берём 28 — большинство и естественная
   * высота строки; иначе первая секция висела бы на 4 pt ниже двух остальных
   * без причины.
   */
  sectionHeaderHeight: 28,
  /**
   * Плитка рубрики (узлы 3566:7275, 3566:7315, 3566:7320, 3566:7325):
   * высота 158, внутреннее поле 12, просвет между надписями 2.
   *
   * ШИРИНЫ ЗДЕСЬ НЕТ, и это не забывчивость. Раньше стояло
   * `rubricCardWidth: 118` — плитка ленты, которая уезжала вбок. Макет
   * переписан на СЕТКУ (ряды 3566:7284 и 3566:7305): колонки объявлены
   * `flex-[1_0_0]`, то есть делят лист поровну, и на кадре 375 это 165.5, а
   * не какое-то нарисованное число. Считает ширину `rubricColumnWidth` в
   * `GuideRubricGrid` — из ширины окна, полей листа и этих двух токенов.
   *
   * Просвет 12 — общий для обеих осей сетки (было 8 в ленте).
   */
  rubricCardHeight: 158,
  rubricCardPadding: 12,
  rubricTextGap: 2,
  rubricGap: 12,
  rubricColumns: 2,
  /** Карточка «Выбора редакции» (node 3192:6266) — 214 высотой, поля 16/12,
   * просвет между строками 5. */
  editorPickHeight: 214,
  /**
   * Карточка гастропрогулки (узлы 3192:6275, 3192:6279) — 206 высотой.
   *
   * СВОЁ ЧИСЛО, а не `listCard.coverHeight`: когда гастрогид рисовали, оба
   * значения были 206, и карточка смотрела на чужой токен. Потом карточка
   * списков уехала на 198 (node 3452:13345), и совпадение кончилось — а
   * гастропрогулка утащила бы за собой чужую правку. Это разные узлы макета,
   * поэтому и числа разные.
   */
  walkHeight: 206,
  /** Поля большой карточки — 16 по бокам, 12 сверху и снизу; просветы между
   * строками 5 у «Выбора редакции» и 4 у гастропрогулки. */
  cardPaddingHorizontal: 16,
  cardPaddingVertical: 12,
  editorPickTextGap: 5,
  walkTextGap: 4,
  /**
   * ЭКРАН РУБРИКИ (node 3492:13723, «Рубрики»). Шапка, лист и заголовок секции
   * у него ТЕ ЖЕ, что у корня гастрогида — числа выше подходят целиком
   * (кадр 340 при статус-баре 44, поля листа 16/24/28, просвет секций 28,
   * просвет «заголовок → содержимое» 12). Своих чисел ровно три.
   */
  /** Просвет «золотая надпись → заголовок» в шапке рубрики — 2 (node
   * 3492:13731, itemSpacing 2). У корня вкладки на этом месте 6
   * (`heroCopyGap`): там три строки стоят одной стопкой, здесь заголовок и
   * строка под ним собраны в отдельную группу. */
  rubricHeroEyebrowGap: 2,
  /** Просвет «заголовок → строка под ним» в шапке рубрики — 4 (node
   * 3496:13835, itemSpacing 4). */
  rubricHeroSublineGap: 4,
  /** Просвет между карточками заведений в списке рубрики — 16 (node
   * 3496:13860, itemSpacing 16; карточки 3496:13854 и 3496:13848 стоят на
   * 18706 и 18920 при высоте 198). СВОЁ число, а не `cardGap` (12): это
   * карточки списка `ListMediaCard`, а не большие карточки гастрогида, и
   * правка одного не должна двигать второе. */
  venueListGap: 16,
} as const;

/**
 * Раскладка страницы бренда в гастрогиде (node 3424:3927, «Ocean Basket»).
 */
export const brandPageLayout = {
  /** Шапка (node 3425:3926) — 357 при статус-баре 0 в макете; содержательная
   * часть считается от верхней безопасной зоны так же, как у гастрогида. */
  heroContentHeight: 357,
  /** Круглая кнопка поверх шапки (node 3427:12226) — 40×40. */
  heroControlSize: 40,
  /** Поля листа (node 3426:9631) — 16 по бокам, 27 сверху и снизу. */
  contentPaddingHorizontal: 16,
  contentPaddingVertical: 27,
  /** Просвет между секциями — 24 (node 3426:9631). */
  sectionGap: 24,
  /** Карта (node 3426:9633) — 240 высотой. */
  mapHeight: 240,
  /** Карточка точки (node 3441:12289): 292 шириной, фотография 215, просвет
   * в ряду 14. */
  venueCardWidth: 292,
  venueCardPhotoHeight: 215,
  venueCardGap: 14,
  /** Пилюля-рубрика в шапке (node 3425:3935) — 36 высотой. */
  heroPillHeight: 36,
  /** Кнопка блока брони (node 3443:12586) — поля 16/12. */
  ctaButtonPaddingHorizontal: 16,
  ctaButtonPaddingVertical: 12,
  /** Блок брони (node 3443:12583) — 150 высотой. */
  ctaHeight: 150,
} as const;

/**
 * ФИРМЕННАЯ СТРАНИЦА OCEAN BASKET — макет 3z0f6dgev4HMwBAHPjTjPo,
 * node 3424:3927 («Ocean Basket / Mobile / 390»).
 *
 * Отдельная группа, а не добавка к `brandPageLayout`: тот описывает ОБЩУЮ
 * страницу подборки гастрогида, которая рисуется для любого слага, а здесь
 * лежат размеры блоков, которых у общей страницы нет вовсе (карта-иллюстрация,
 * блюда, главы истории, блок инстаграма). Кадр макета — 390 шириной, поэтому
 * всё, что тянется по ширине, в коде считается от окна, а числа ниже — это
 * ВЫСОТЫ, скругления и просветы, которые от ширины не зависят.
 */
export const oceanPageLayout = {
  /**
   * Шапка (node 3425:3926) — 327 при статус-баре 0.
   *
   * БЫЛО 357: число сняли с ранней версии кадра. Пересчитано 2026-09-03 по
   * сырому JSON узла (`absoluteBoundingBox` 390×327, нижние углы 24) — все
   * дети шапки ниже приведены к тому же кадру: пилюля поднялась на 22,
   * надпись и плашка промо — на 30, свечение осталось на месте.
   */
  heroHeight: 327,
  /** Нижние углы шапки (node 3425:3926, `rectangleCornerRadii [0,0,24,24]`). */
  heroBottomRadius: 24,
  /** Боковые поля содержимого шапки — 24 с каждой стороны (контентный блок
   * 5012:5189 — 342 при кадре 390). Ряд кнопок шире, у него поля 20. */
  heroContentPaddingHorizontal: 24,
  /** Свечение за надписью (node 3425:3927) — круг 210, левый край на 90,
   * верхний на 145 от верха шапки. */
  heroGlowSize: 210,
  heroGlowLeft: 90,
  heroGlowTop: 145,
  /** Пилюля «КАРТА ПРИКЛЮЧЕНИЙ» (node 3425:3935) — 268×36, скругление 19,
   * верх на 82 (26 → панель 40 → 16). */
  heroPillWidth: 268,
  heroPillHeight: 36,
  heroPillRadius: 19,
  heroPillTop: 82,
  /** Надписи «Seafood» и «Expedition» (узлы 3425:3940, 3425:3941) — размеры
   * и отступы НАРИСОВАННОГО (absoluteRenderBounds: 133.9×43.2 на y 144.5 и
   * 185.3×48.1 на y 184.9), потому что в коде это картинки, а не текст:
   * шрифта Lobster в приложении нет. */
  heroSeafoodWidth: 134,
  heroSeafoodHeight: 43,
  heroSeafoodTop: 144.5,
  heroExpeditionWidth: 185,
  heroExpeditionHeight: 48,
  heroExpeditionTop: 185,
  /** Плашка «WELCOME DRINK» (node 3425:3942) — 342×48, скругление 24, верх
   * на 255 (низ на 303, до низа шапки 24), кружок значка 30, разделитель
   * 1×18. */
  welcomeWidth: 342,
  welcomeHeight: 48,
  welcomeRadius: 24,
  welcomeTop: 255,
  welcomeIconCircle: 30,
  welcomeDividerHeight: 18,
  /**
   * ШТОРКА «WELCOME DRINK» (узел 5012:5670 в кадре 5012:5489, снято
   * 2026-09-03): верхние углы 24, ручка 40×4 на 6 от верха, герой 170 со
   * скруглением 24, плашка промо 32 со скруглением 15, карточка 82 со
   * скруглением 24 и кружком значка 36, галочка 20, плитка шага 48 со
   * скруглением 8, блок условий со скруглением 16.
   */
  welcomeSheetRadius: 24,
  welcomeSheetHandleWidth: 40,
  welcomeSheetHandleHeight: 4,
  welcomeSheetHandleTop: 6,
  /** В макете 170; +42 — крестик закрытия (40) с просветом (8) над плашкой промо
   * вместо макетных 22 воздуха сверху, см. OceanWelcomeDrinkSheet. */
  welcomeSheetHeroHeight: 212,
  welcomeSheetPromoHeight: 32,
  welcomeSheetPromoRadius: 15,
  welcomeSheetCardMinHeight: 82,
  welcomeSheetCardRadius: 24,
  welcomeSheetCardIconCircle: 36,
  welcomeSheetCheckSize: 20,
  welcomeSheetStepTile: 48,
  welcomeSheetStepTileRadius: 8,
  welcomeSheetTermsRadius: 16,
  /** Карта-иллюстрация (node 3426:9633) — 240 высотой, скругление 22. */
  mapHeight: 240,
  mapRadius: 22,
  /** Карточка точки (node 3441:12289) — 292×291, фотография 215, скругление
   * 16, просвет ленты 14; кружок номера 27, пилюля «Welcome drink» 138×30
   * (скругление 15), круглая кнопка с рыбой 30. */
  venueCardWidth: 292,
  venueCardHeight: 291,
  venueCardPhotoHeight: 215,
  venueCardRadius: 16,
  venueCardGap: 14,
  venueBadgeSize: 27,
  venueTagHeight: 30,
  venueTagRadius: 15,
  venueFishButton: 30,
  /** Карточка блюда (узлы 3441:12384, 3441:12389) — 175 шириной (вторая 177,
   * разница нарисована случайно), фотография 108, скругление 14, просвет 12. */
  dishCardWidth: 175,
  dishPhotoHeight: 108,
  dishCardRadius: 14,
  dishCardGap: 12,
  /** Глава истории (узлы 3443:12468, 3443:12480) — скругление 16, шапка с
   * полями 16, свёрнутая карточка 82 высотой (поля 18/16), раскрытая часть
   * 173, кружок значка 42. */
  storyCardRadius: 16,
  storyHeaderPadding: 16,
  storyCollapsedPaddingVertical: 18,
  storyCollapsedHeight: 82,
  storyBodyHeight: 173,
  storyIconCircle: 42,
  storyGap: 12,
  /** Замыкающая связка (узлы 3443:12562…3443:12587): круг с якорем 66,
   * блок инстаграма 76 высотой со скруглением 16, значок 42 со скруглением 12,
   * блок брони 150 высотой со скруглением 22, кнопка со скруглением 22 и
   * полями 16/12. */
  anchorCircle: 66,
  anchorRingWidth: 6,
  instagramHeight: 76,
  instagramRadius: 16,
  instagramIconSize: 42,
  instagramIconRadius: 12,
  ctaHeight: 150,
  ctaRadius: 22,
  ctaButtonRadius: 22,
  ctaButtonPaddingHorizontal: 16,
  ctaButtonPaddingVertical: 12,
} as const;

/** Stroke widths from the design. */
export const borderWidth = {
  /** Time-slot pill outline — 1.5px, grey when idle, brand when chosen
   * (Figma node 471:3914). */
  control: 1.5,
} as const;
