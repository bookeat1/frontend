/**
 * Color tokens — verified against the Figma file (fileKey 7rBjjTjp4FbxV9SCJmypWF),
 * node 340:2535 ("Карточка заведения") plus the search/photo screens.
 * Named by semantic role, not by raw value, so screens never hardcode hex.
 */
export const colors = {
  brand: {
    /** Primary CTA fill, active banner outline, map pin, active nav icon. */
    primary: "#B33036",
    /**
     * Залитое сердечко у сохранённой карточки — #B33036 (Figma
     * 3z0f6dgev4HMwBAHPjTjPo, экран «Избранное», node 3053:10169: узлы
     * 3593:8608, 3593:8611, 3593:8614 — `fill: #B33036`).
     *
     * БЫЛО #FF3838. Это значение никогда не приходило из макета: его сняли
     * ПИПЕТКОЙ с картинки-референса (`design-ref/screen-explore.png`), и
     * поэтому оно оказалось ярче фирменного. Макет от 2026-09-01 называет
     * цвет явно, и он совпадает с `brand.primary`.
     *
     * Токен оставлен своим, а не заменён на `brand.primary`: это РОЛЬ
     * («сохранено»), и если дизайнер разведёт её с цветом кнопки, менять
     * придётся одно место, а не все сердечки поимённо.
     */
    favorite: "#B33036",
  },
  text: {
    /** Default body/heading text. */
    primary: "#1B1B1B",
    /** Secondary/disabled text — addresses subtext, inactive tabs, captions. */
    muted: "#A5A5A5",
    /** Slightly darker muted tone used for chip labels in search results. */
    mutedStrong: "#7D7D7D",
    /**
     * Подпись чипа-метки ВНУТРИ карточки — #96272C (Figma
     * 3z0f6dgev4HMwBAHPjTjPo, node 3053:8540). Тёмная бордовая, а не
     * `brand.primary` (#B33036): на светлой бордовой подложке чипа сам
     * фирменный цвет даёт слишком низкий контраст, и макет называет именно
     * этот, более тёмный оттенок.
     */
    brand: "#96272C",
    /** Подпись под заголовком экрана — тёмная, не приглушённая. Отдельный
     * токен, потому что в макете это #171717, а не общий text.primary
     * (#1B1B1B): оттенки соседние, но макет называет именно этот. */
    subtitle: "#171717",
    /** Pure black used specifically for menu item name/price in the design. */
    strong: "#000000",
    /** Text placed directly over photos/scrim. */
    onDark: "#FFFFFF",
    /** Подпись месяца под числом на кадре афиши (node 3053:8888) — #E2E2E2.
     * Приглушённо-белая: месяц вторичен рядом с числом, но всё ещё лежит на
     * фотографии, где обычный серый нечитаем. */
    onPhotoMuted: "#E2E2E2",
    /**
     * Подпись «заведение · дата · время» на кадре афиши — белый с
     * прозрачностью 85 % (Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3452:13202
     * карточки списка и 3452:13245 карточки афиши). Отдельный токен, а не
     * `onPhotoMuted` (#E2E2E2, сплошной): здесь макет называет прозрачность,
     * и подпись слегка пропускает фотографию под собой.
     */
    onPhotoSubtitle: "rgba(255, 255, 255, 0.85)",
    /** Text on the brand-colored primary button. */
    onBrand: "#FFFFFF",
    /**
     * Подпись ПОД названием на снимке карточки списка — белый с прозрачностью
     * 85% (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3452:13350:
     * `rgba(255,255,255,0.85)`). Не чистый `onDark`: строка «Европейская ·
     * ₸₸₸» вторична рядом с именем места, и в макете она приглушена.
     */
    onDarkMuted: "rgba(255, 255, 255, 0.85)",
    /**
     * Неактивная вкладка нижней навигации — #595959 из макета
     * (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3039:23943). Отдельный токен, а не
     * `muted` (#A5A5A5): на полупрозрачной плашке светло-серая подпись в 10 pt
     * читается хуже, макет рисует именно этот, более тёмный серый.
     */
    navInactive: "#595959",
  },
  background: {
    /** Screen background behind white surface cards. */
    screen: "#F5F5F5",
    /** Card / sheet surface. */
    surface: "#FFFFFF",
    /**
     * Тот же белый лист, но прозрачный — начало затухания у края
     * горизонтального ряда. Отдельный токен, потому что «прозрачный» в
     * градиенте обязан быть ТЕМ ЖЕ цветом с нулевой прозрачностью: чистый
     * `transparent` на iOS даёт серую кайму по краям перехода.
     */
    surfaceTransparent: "rgba(255, 255, 255, 0)",
    /** Neutral pill chip background (hours, price, menu tag chips). */
    chip: "#F1F1F1",
    /**
     * Приглушённая подложка блока внутри белого экрана — #F8F8F8 (в макетах
     * это переменная `background/subtle`): карточка брони, вторичная кнопка,
     * дорожка переключателя вкладок. Светлее фона экрана (#F5F5F5), потому что
     * лежит НА белом, а не под ним.
     */
    subtle: "#F8F8F8",
    /** Slightly different chip background used on the search bar + filter row. */
    chipAlt: "#F3F2F2",
    /**
     * Подложка чипа-метки внутри карточки — фирменный цвет с прозрачностью
     * 15% (Figma node 3053:8539, rgba(179,48,54,0.15)). Заменил прежнюю серую
     * подложку по правке владельца от 2026-08-20: «чипы сейчас все красного
     * цвета».
     *
     * ЭТО НЕ ЧИП-ФИЛЬТР. Ряды «На сегодня / Рестораны / События» остались
     * прежними: выбранный сплошной `brand.primary` с белым текстом,
     * невыбранный светло-серый. Разный смысл — разный вид: фильтр это
     * переключатель, а чип в карточке это подпись.
     */
    chipBrand: "rgba(179, 48, 54, 0.15)",
    /** Selected/active filter chip background. */
    chipActive: "#1B1B1B",
    /** "Посмотреть меню" secondary button fill. */
    secondaryButton: "#E5E5E5",
    /** Circular social-icon button fill. */
    socialIcon: "#F2F2F2",
    /** Placeholder fill behind promo banner images while loading. */
    bannerPlaceholder: "#E7E7E7",
    /**
     * Solid fill of the rebuilt home header (Figma home design, 2026-08-06).
     * The design draws a dark restaurant photo behind the greeting, but the
     * backend has no home-header image endpoint yet, so the header is a flat
     * dark surface instead of a fabricated/placeholder photo. Dark charcoal so
     * the white greeting, city and bell stay legible without a scrim.
     */
    header: "#2A2321",
    /** Full-bleed photo viewer backdrop (Figma node 340:2455). */
    photoViewer: "#000000",
    /**
     * Fill behind the floating tab bar where the native liquid-glass effect is
     * unavailable (iOS below 26, Android). Translucent rather than solid so the
     * bar still reads as a layer above the content scrolling under it, while
     * staying opaque enough for the 10pt tab labels.
     */
    navBarFallback: "rgba(255, 255, 255, 0.94)",
    /**
     * Заливка плашки нижней навигации из макета: белый 30 %. Работает только
     * поверх НАСТОЯЩЕГО размытия (iOS 26, `expo-glass-effect`) — там это
     * оттенок стекла. Без размытия столько прозрачности означает подпись
     * поверх голой фотографии, поэтому используется `navBarFallback`.
     */
    navBarGlassTint: "rgba(255, 255, 255, 0.3)",
  },
  /**
   * ГАСТРОГИД — палитра «Editorial v2» (Figma 3z0f6dgev4HMwBAHPjTjPo,
   * node 3192:6246) и страницы бренда (node 3424:3927).
   *
   * Отдельная группа, а не добавка в `text`/`background`: у гастрогида в новом
   * макете своя журнальная гамма (кремовый лист, золото по фотографии,
   * почти-чёрные заголовки), и подмешать её в общие роли значило бы утащить
   * этот вид на главную и в поиск.
   */
  guide: {
    /** Лист экрана гастрогида — #F7F5F1 (node 3192:6246). Кремовый, а не общий
     * серый `background.screen` (#F5F5F5): под фотографиями и золотом холодный
     * серый читался бы как выцветший. */
    sheet: "#F7F5F1",
    /** Золото подписей поверх фотографии — #EDD19E (узлы 3192:6254, 3192:6099,
     * 3192:6268). */
    gold: "#EDD19E",
    /** Заголовки секций — #1B1B1B (узлы 3492:13477, 3492:13479, 3492:13486,
     * 3492:13743; в макете это стиль Neutral-900). БЫЛО #141414 по старому
     * узлу 3192:6260 — переписанные заголовки секций стоят на общем цвете
     * текста. Токен оставлен своим (а не заменён на `text.primary`), чтобы
     * гамма гастрогида читалась одной группой; значения совпали. */
    headline: "#1B1B1B",
    /** «Смотреть все» — #B23036 (node 3192:6261). ВНИМАНИЕ: это НЕ
     * `brand.primary` (#B33036) — макет отличается на один разряд. Отдельный
     * токен именно поэтому: подгонять его к фирменному значит молча
     * переписывать макет. */
    link: "#B23036",
    /** Строка под названием «Выбора редакции» — #EBEBEB (node 3192:6270).
     * Светло-серая по фотографии: она вторична рядом с белым названием. */
    cardMeta: "#EBEBEB",
    /** Описание на карточке гастропрогулки — белый 75 % (node 3192:6278). */
    cardBody: "rgba(255, 255, 255, 0.75)",
    /**
     * Затемнения поверх фотографий. Все три — вертикальные градиенты из
     * прозрачного в чёрный, но с РАЗНЫМИ плотностью и точкой старта, потому
     * что на карточках лежит разный объём текста:
     *   • плитка рубрики (3566:7275 и соседи) — 0.9, старт 49.5 %
     *     (БЫЛО 0.8 по старому узлу 3192:6097: макет перерисован вместе с
     *     переходом ленты в сетку);
     *   • «Выбор редакции» (3192:6266) — тёмно-синий #0A3D6E, старт 0 %;
     *   • гастропрогулка (3192:6275) — 0.82, старт 25 %.
     */
    rubricScrimEnd: "rgba(0, 0, 0, 0.9)",
    /**
     * «Выбор редакции» (node 3192:6266) гасится НЕ чёрным, а тёмно-синим
     * #0A3D6E от самого верха кадра. БЫЛО `rgba(0, 0, 0, 0.85)` — макет
     * перерисован.
     *
     * ВНИМАНИЕ: это цвет ОДНОГО бренда (Ocean Basket, чья морская гамма
     * живёт в `brand2`), а карточка рисуется для ЛЮБОЙ подборки: поля с
     * фирменным цветом в ответе `GET /gastroguide/collections` нет. Синева
     * ляжет на все карточки секции, как нарисовано.
     */
    editorPickScrimEnd: "#0A3D6E",
    /** Прозрачный конец градиента «Выбора редакции» — тот же синий с нулевой
     * прозрачностью (node 3192:6266: `rgba(10, 61, 110, 0)`), а не чёрный:
     * иначе переход уходил бы в серость. */
    editorPickScrimStart: "rgba(10, 61, 110, 0)",
    walkScrimEnd: "rgba(0, 0, 0, 0.82)",
    /** Прозрачный конец любого из этих градиентов. Именно чёрный с нулевой
     * прозрачностью, а не `transparent`: на iOS чистый `transparent` даёт
     * серую кайму по переходу. */
    scrimStart: "rgba(0, 0, 0, 0)",
    /**
     * Шапка гастрогида (node 3192:6247) затемнена ДВУМЯ слоями сразу:
     * сплошным чёрным 20 % по всему кадру и вертикальным градиентом снизу
     * вверх — 74 % у нижней кромки, прозрачно к 65 % высоты.
     */
    heroFlatScrim: "rgba(0, 0, 0, 0.2)",
    heroGradientBottom: "rgba(0, 0, 0, 0.74)",
    heroGradientTop: "rgba(0, 0, 0, 0)",
  },
  /**
   * СТРАНИЦА БРЕНДА в гастрогиде (node 3424:3927, «Ocean Basket»).
   *
   * Морская гамма: тёмно-синий и золото. Это НЕ вторая тема приложения —
   * она живёт ровно на одном экране, поэтому и вынесена отдельной группой,
   * а не подмешана в общие роли.
   */
  brand2: {
    /** Лист страницы — #FCF7EC (node 3424:3927). Теплее кремового листа
     * гастрогида (#F7F5F1): страница подана как бумага. */
    sheet: "#FCF7EC",
    /** Основной синий: заголовки, названия точек, подложка кнопок
     * (узлы 3426:9632, 3441:12293). */
    navy: "#052747",
    /** Заливка блока брони — #063C61 (node 3443:12583), светлее `navy`. */
    navyDeep: "#063C61",
    /** Золото: подписи, кнопка «Забронировать», рамки (узлы 3425:3939,
     * 3441:12292, 3443:12586). */
    gold: "#D2C159",
    /** Приглушённое золото подписей глав истории — #AD993D (node 3443:12476).
     * Темнее `gold`: оно лежит на белом, где #D2C159 нечитаемо. */
    goldMuted: "#AD993D",
    /** Светлый текст поверх синего — #FBF8F0 (node 3425:3940). */
    onNavy: "#FBF8F0",
    /** Вторичный текст на белой карточке — #546B80 (node 3443:12580). */
    muted: "#546B80",
    /** Рамка белой карточки главы — #DBE3EB (node 3443:12468). */
    cardBorder: "#DBE3EB",
    /** Подложка блока Instagram — #F6EAD4 (node 3443:12573). */
    accentSurface: "#F6EAD4",
    /** Полупрозрачная подложка круглых кнопок поверх шапки — белый 30 %
     * (node 3427:12226). */
    heroControl: "rgba(255, 255, 255, 0.3)",
    /** Подложка пилюли-рубрики в шапке — белый 5 % (node 3425:3935). Почти
     * невидима: пилюлю держит золотая обводка, а не заливка. */
    pillSurface: "rgba(255, 255, 255, 0.05)",
    /** Обводка пилюли-рубрики — золото 35 % (node 3425:3935). */
    pillBorder: "rgba(210, 193, 89, 0.35)",
    /** Обводка блока «welcome drink» — золото 45 % (node 3425:3942). */
    accentBorder: "rgba(210, 193, 89, 0.45)",
    /** Градиент шапки страницы бренда (node 3425:3926) — три точки. */
    heroGradient: ["#05182D", "#052747", "#033C61"],
    /**
     * ФИРМЕННАЯ СТРАНИЦА OCEAN BASKET (node 3424:3927) — цвета, которых не
     * было у общей страницы подборки. Все сняты с узлов этого кадра, номера
     * рядом с каждым.
     */
    /** Рыбы-силуэты в шапке (узлы 3443:12520…3443:12528) — #8C947A с
     * прозрачностью 66 %, как в макете. */
    heroFish: "rgba(140, 148, 122, 0.66)",
    /** Свечение за надписью «Seafood Expedition» (node 3425:3927) — радиальный
     * градиент от #1F8CC7 38 % к #052647 0 %. Именно #052647 с нулевой
     * прозрачностью, а не `transparent`: см. `scrimStart` выше. */
    heroGlowStart: "rgba(31, 140, 199, 0.38)",
    heroGlowEnd: "rgba(5, 38, 71, 0)",
    /** Плашка «WELCOME DRINK» в шапке (node 3425:3942) — #1A476E 94 %. */
    welcomeSurface: "rgba(26, 71, 110, 0.94)",
    /** Разделитель внутри этой плашки (node 3425:3947) — золото 50 %. */
    welcomeDivider: "rgba(210, 193, 89, 0.50)",
    /** Тёмная краска ПОВЕРХ золота: значок подарка и рыбы на золотой пилюле
     * (узлы 3425:3945, 3441:12302) — #062B4D, чуть глубже `navy`. */
    navyInk: "#062B4D",
    /** Обводка кружка главы истории (node 3443:12471) — #C9B84F. */
    goldRing: "#C9B84F",
    /** Значок внутри этого кружка (node 3443:12473) — #C8B956. */
    goldIcon: "#C8B956",
    /** Стрелка-шеврон главы и стрелка блока инстаграма (узлы 3443:12479,
     * 3443:12582) — #AF9F3D. */
    goldChevron: "#AF9F3D",
    /** Текст раскрытой главы поверх фотографии (node 3443:12605) — #FFF4C5. */
    storyBody: "#FFF4C5",
    /** Средняя точка затемнения под этим текстом (node 3443:12597) — тот же
     * `navy` с прозрачностью 45 % на 51 % высоты. */
    storyScrim: "rgba(5, 39, 71, 0.45)",
    /** Заголовок замыкающего блока (node 3443:12585) — #FEFAF2. Светлее
     * `onNavy`, и это разные узлы, а не округление. */
    ctaOnNavy: "#FEFAF2",
    /** Рамка и тень карточки-карты (node 3426:9633). */
    mapBorder: "rgba(199, 209, 212, 0.65)",
    mapShadow: "rgba(13, 31, 46, 0.12)",
    /** Обводка круга с якорем в замыкающем блоке (node 3443:12566) — золото
     * 22 % толщиной 6. */
    anchorRing: "rgba(210, 193, 89, 0.22)",
    /** Шеврон «Подробнее» в шапке (node 3425:3950) — #FFF8EB. */
    chevronLight: "#FFF8EB",
  },
  /**
   * Пилюля статуса брони. Значения сверены с макетом «Мои брони»
   * (Figma dVjT37j984ErvOmzxlx29p, node 3004:6781): там нарисованы все три
   * рабочих тона — pending (3004:6814), confirmed (3004:6830) и cancelled
   * (3004:6872). До этого тона брались с экрана деталки брони
   * (oPxXynSOY3PYhf3gkVR5Ps, node 488:9876), где красного не было вовсе и
   * `negative` был нашим.
   *
   * `neutral` (статус `completed`) в макете по-прежнему НЕ нарисован — он
   * остаётся нашим: приглушённый серый из существующей палитры.
   *
   * Осторожно: контраст текста к подложке у всех трёх тонов ниже 4.5:1
   * (примерно 2.5–3.1:1 при кегле 12). Это значения макета, а не наш выбор;
   * если понадобится доступный вариант — менять надо макет, а не только код.
   */
  status: {
    /** pending / waitlist — заведение ещё не ответило. */
    pendingText: "#F67700",
    pendingSurface: "#FFE4CC",
    /** confirmed / arrived — стол ваш. */
    positiveText: "#16A34A",
    positiveSurface: "#D7F9E3",
    /** cancelled / no_show — бронь мертва. */
    negativeText: "#FF323B",
    negativeSurface: "#FFE9EA",
    /**
     * Тот же смысл, но для текста на БЕЛОМ листе (сообщение об ошибке в
     * диалоге отмены брони). Пилюльный #FF323B на белом даёт около 3.4:1 —
     * ниже нормы для обычного текста, — поэтому тут остаётся тёмный красный,
     * который раньше был `negativeText`.
     */
    negativeTextOnSurface: "#B33036",
    /** completed — визит закончился, и ничего не сломалось. */
    neutralText: "#7D7D7D",
    neutralSurface: "#F1F1F1",
    /**
     * ПОЛОСКА статуса под карточкой брони — новый вид списка «Брони»
     * (Figma 3z0f6dgev4HMwBAHPjTjPo, узлы 3589:8230 и 3589:8307: карточка
     * лежит в рамке со скруглением 24 и нижним полем 6, а заливка рамки
     * выглядывает снизу цветной полосой).
     *
     * ЭТО НЕ `positiveText`/`pendingText`. Те два — цвета ПОДПИСИ на светлой
     * подложке пилюли; здесь цвет заливки самой полосы, и макет называет для
     * неё другие значения. Один токен на две роли развалился бы при первой же
     * правке любой из них.
     *
     * В макете нарисованы ровно два: зелёный у подтверждённой брони и жёлтый
     * у ожидающей. Больше и не нужно — полоса рисуется только во вкладке
     * «Активные», где бронь бывает лишь `pending`/`waitlist` (жёлтая) или
     * `confirmed`/`arrived` (зелёная). У карточки истории (узел 3589:8529)
     * полосы нет вовсе.
     */
    stripPositive: "#22CB87",
    stripPending: "#F3BE2D",
  },
  border: {
    /** Hairline between content and bottom nav / sticky footer. */
    subtle: "#F5F5F5",
    /** 1.5px outline of an unselected time-slot pill — Figma file
     * oPxXynSOY3PYhf3gkVR5Ps, node 471:3914 (Reservation, slots card). */
    control: "#E5E5E5",
  },
  overlay: {
    /** Scrim behind icon buttons placed on photos (back/close). */
    scrim: "rgba(0, 0, 0, 0.55)",
    /**
     * Круглая подложка под кнопкой ПОВЕРХ фотографии — сердечко избранного
     * (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3053:8506). Белая с прозрачностью
     * 20%: держит иконку читаемой и на светлом блюде, и на тёмном зале.
     */
    photoControl: "rgba(255, 255, 255, 0.2)",
    /**
     * Затемнение под датой на кадре карточки афиши (node 3053:8885) —
     * rgba(0,0,0,0.3). Легче общего `scrim`: под ним лежит только дата в две
     * строки, а не текст во всю карточку, и более плотная заливка убила бы
     * саму фотографию.
     */
    photoDate: "rgba(0, 0, 0, 0.3)",
    /** Bottom gradient over promo banner photos so caption text stays legible. */
    bannerGradientTop: "rgba(0, 0, 0, 0)",
    bannerGradientBottom: "rgba(0, 0, 0, 0.7)",
    /** Sticky booking-button footer shadow. */
    footerShadow: "rgba(0, 0, 0, 0.08)",
    /** Backdrop behind a modal confirmation dialog. Darker than `scrim`
     * because it covers a white screen, not a photo. */
    dialogScrim: "rgba(0, 0, 0, 0.45)",
    /**
     * Затемнение поверх фотографии в шапке главной. Ровно чёрный с 20 %, как
     * в макете (3z0f6dgev4HMwBAHPjTjPo, node 3102:11986: поверх заливки-кадра
     * лежит сплошная чёрная с прозрачностью 0.2). Было 0.45 — под прежний,
     * более светлый кадр; новый (бокал у бархатного занавеса) сам по себе
     * тёмный, средняя яркость под приветствием ~60 из 255, и белый текст
     * читается уже при 20 %.
     */
    headerScrim: "rgba(0, 0, 0, 0.2)",
    /**
     * Затемнение поверх фотографии в шапке гастрогида (макет
     * dVjT37j984ErvOmzxlx29p, node 1099:6802 — `rgba(0,0,0,0.3)`). Легче, чем
     * `headerScrim` (0.2): на этом кадре светлый только верх, и заголовок в
     * 24 pt требует более плотного затемнения.
     */
    guideHeroScrim: "rgba(0, 0, 0, 0.3)",
    /**
     * Затемнение снимка карточки списка нового вида — три точки из макета
     * (node 3452:13345): сверху прозрачное, в середине rgba(0,0,0,0.2), внизу
     * rgba(0,0,0,0.86). Плотнее, чем `bannerGradient*`: на этом снимке лежат
     * ДВЕ строки текста, и нижняя треть должна быть почти чёрной, иначе имя
     * места пропадает на светлом зале.
     */
    listCardScrimTop: "rgba(0, 0, 0, 0)",
    listCardScrimMid: "rgba(0, 0, 0, 0.2)",
    listCardScrimBottom: "rgba(0, 0, 0, 0.86)",
    /**
     * Круглая подложка кнопки избранного на карточке списка — серый с
     * прозрачностью 50% (node 3452:13346: `bg-[rgba(177,177,177,0.5)]`).
     * Отдельно от `photoControl` (белый 20%): у карточки нового вида нижняя
     * половина снимка почти чёрная от градиента, и белая подложка на ней
     * светилась бы пятном.
     */
    listCardControl: "rgba(177, 177, 177, 0.5)",
    /**
     * Стеклянная пилюля ПОВЕРХ снимка карточки брони — «2 гостя», адрес
     * (Figma 3z0f6dgev4HMwBAHPjTjPo, node 3589:8234: `bg-[rgba(255,255,255,0.3)]`
     * плюс размытие фона 20).
     *
     * Плотнее, чем `photoControl` (белый 20 %): под сердечком лежит только
     * глиф, а тут ещё и подпись в 12 pt, которой нужен более плотный фон.
     * Нативного размытия под ней нет по той же причине, что и у сердечка: на
     * Android и на iOS до 26 его не существует, и половина устройств увидела
     * бы вместо стекла мутный прямоугольник.
     */
    photoPill: "rgba(255, 255, 255, 0.3)",
    /**
     * РОВНОЕ затемнение снимка карточки прошедшей брони — чёрный 60 %
     * (node I3589:8529;3589:8486). Именно заливка, а не градиент
     * `listCardScrim*`: карточка истории высотой 93 несёт текст и сверху, и
     * снизу, и градиент оставил бы верхнюю строку на голой фотографии.
     */
    photoFlatScrim: "rgba(0, 0, 0, 0.6)",
    /**
     * Затемнение шапки карточки заведения — от прозрачного через
     * rgba(0,0,0,0.1) на половине высоты к чёрному внизу (node 3446:12623).
     * Плотнее к низу, чем `headerScrim`: под ним лежит имя в 28 pt, подпись и
     * ряд меток, а не одна строка приветствия.
     */
    venueHeroScrimTop: "rgba(0, 0, 0, 0)",
    venueHeroScrimMid: "rgba(0, 0, 0, 0.1)",
    venueHeroScrimBottom: "rgba(0, 0, 0, 1)",
    /** Page dots over the Explore hero carousel (design-ref/screen-explore.png):
     * the current page is a solid white capsule, the rest are dimmed dots. */
    carouselDot: "rgba(255, 255, 255, 0.55)",
    carouselDotActive: "#FFFFFF",
    /**
     * Вертикальный градиент под плавающей плашкой навигации
     * (`linear-gradient(rgba(239,239,239,0), rgba(255,255,255,0.65))` в
     * макете): контент, уезжающий под плашку, гасится к низу экрана.
     */
    navBarGradientTop: "rgba(239, 239, 239, 0)",
    navBarGradientBottom: "rgba(255, 255, 255, 0.65)",
    /** Рамка плашки навигации — `1px solid rgba(255,255,255,0.14)`. */
    navBarBorder: "rgba(255, 255, 255, 0.14)",
    /**
     * Затемнение кадра КАРТОЧКИ СПИСКА «Афиша» (node 3452:13199): сверху
     * прозрачное, на середине 20 %, у нижнего края 86 %. Три стопа, а не два:
     * подпись лежит в самом низу кадра, и линейное затухание из прозрачного в
     * 0.86 съедало бы фотографию уже на середине.
     */
    eventCardGradient: ["rgba(0, 0, 0, 0)", "rgba(0, 0, 0, 0.2)", "rgba(0, 0, 0, 0.86)"],
    /**
     * Затемнение кадра КАРТОЧКИ АФИШИ (node 3452:13227): прозрачное → 10 % на
     * 49.8 % высоты → сплошной чёрный у низа. Плотнее, чем у карточки списка:
     * поверх него лежит название в 28 pt, а кадр вдвое выше.
     */
    eventHeroGradient: ["rgba(0, 0, 0, 0)", "rgba(0, 0, 0, 0.1)", "#000000"],
    /**
     * Круглая подложка плавающих кнопок на кадре афиши (node 3452:13234) —
     * белая с прозрачностью 92 %. Не `photoControl` (20 %): в этом макете
     * кнопки почти непрозрачные, а глиф в них тёмный.
     */
    photoControlLight: "rgba(255, 255, 255, 0.92)",
    /**
     * Подложка метки-пилюли на кадре афиши (node 3452:13249) — белая с
     * прозрачностью 30 %, с размытием под ней.
     */
    heroPill: "rgba(255, 255, 255, 0.3)",
  },
} as const;

export type ColorToken = typeof colors;
