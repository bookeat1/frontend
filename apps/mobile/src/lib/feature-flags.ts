/**
 * Выключатели незавершённых кусков интерфейса.
 *
 * Здесь лежат ТОЛЬКО те переключатели, которые прячут уже написанный экранный
 * код, пока за ним нет данных или ключа. Смысл файла — чтобы возврат стоил
 * одну строку (`false` → `true`), а не археологию по истории git: код остаётся
 * на месте, компилируется и покрыт тестами, но гость его не видит.
 *
 * Правило: у каждого флага в комментарии написано, ПОЧЕМУ он выключен и ЧТО
 * должно произойти, чтобы его включить. Флаг без такой записи здесь не нужен —
 * он превращается в мусор, который никто не решается тронуть.
 *
 * Это КОНСТАНТЫ сборки, а не настройки времени выполнения: удалённого
 * конфига в приложении нет, и заводить его ради трёх временно спрятанных
 * блоков не стоит. Включение приезжает обычным релизом или OTA.
 */

/**
 * Счётчики «Отзывов» и «Друзья» на экране «Профиль».
 *
 * ВЫКЛЮЧЕНО (правка владельца 28.08.2026): ни отзывов, ни друзей в продукте
 * нет вовсе — ни ручки, ни экрана. У любого гостя эти плашки показывали
 * честный, но бессмысленный 0, и профиль выглядел так, будто человек ничего
 * не сделал. Остаётся один счётчик — «Брони», за которым есть `GET /bookings`.
 *
 * ВКЛЮЧИТЬ, когда у бэкенда появятся отзывы гостя и друзья: вернуть `true` и
 * передать в `ProfileStats` настоящие числа вместо нулей в `app/profile.tsx`.
 */
export const PROFILE_SOCIAL_STATS_ENABLED = false;

/**
 * Строка «Безопасность» в «Настройках».
 *
 * ВЫКЛЮЧЕНО (правка владельца 28.08.2026): экрана безопасности не существует,
 * строка стояла неинтерактивной с подписью «Скоро». Обещание в меню, за
 * которым ничего не открывается, раздражает сильнее, чем отсутствие пункта, —
 * ровно по этой причине из профиля уже убрали остальные строки «Скоро».
 *
 * ВКЛЮЧИТЬ вместе с экраном безопасности: вернуть `true` и заменить `InfoRow`
 * на обычную строку с переходом.
 */
export const SETTINGS_SECURITY_ROW_ENABLED = false;

/**
 * Картинка карты под контактами (`MapPreview`) — карточка заведения, карточка
 * афиши, карточка акции и блок контактов в брони.
 *
 * ВЫКЛЮЧЕНО (правка владельца 28.08.2026): поставщик карт на бэкенде не
 * подключён, `GET /restaurants/:id/map` отвечает 503 `map_not_configured`, и
 * гость видит серую пунктирную заглушку вместо карты. Заглушка честная, но
 * бесполезная: она занимает 180 pt и ничего не сообщает.
 *
 * Адрес и переход «открыть в картах» это НЕ трогает — они живут в
 * `VenueAddressRow` и работают без нашей картинки.
 *
 * ВКЛЮЧИТЬ, как только на бэкенде появится ключ Geoapify: вернуть `true`.
 * Больше ничего делать не нужно — вся логика загрузки, кэша и разбора отказа
 * (`src/lib/map-preview.ts`) осталась на месте.
 */
export const MAP_PREVIEW_ENABLED = false;
