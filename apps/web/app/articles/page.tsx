import { ArticlesScreen } from "@web/components/articles/ArticlesScreen";

/** Список статей — узел 5033:7382. Состояние живёт в запросе, параметров адреса нет. */
export default function ArticlesPage() {
  return <ArticlesScreen />;
}
