import { getDictionary } from "@bookeat/i18n";

/**
 * Единственная точка доступа к словарю в вебе. RU — язык по умолчанию;
 * переключатель языка появится вместе с экранами, и тогда этот модуль станет
 * контекстом, а не константой. Компоненты пишут `t.web.*` и НИКОГДА не держат
 * русский текст в разметке — полноту kk/en стережёт тест
 * packages/i18n/src/__tests__/completeness.test.ts.
 */
export const t = getDictionary("ru");
