/**
 * `DEFAULT_CITY` живёт в СВОЁМ модуле БЕЗ `"use client"` — не ради стиля.
 *
 * Баг, который это правило предотвращает (проверено сборкой T1
 * `web-ai-search-visibility-20260918.md`): `city.tsx` помечен `"use client"`,
 * и когда СЕРВЕРНЫЙ компонент (`app/page.tsx`, `app/venues/page.tsx`)
 * импортировал `DEFAULT_CITY` оттуда напрямую, Next заворачивает КАЖДЫЙ
 * экспорт клиентского модуля в клиентскую ссылку — даже простую строковую
 * константу. На сервере это дало `undefined` МОЛЧА (TypeScript не видит
 * разницы, рантайм — видит): `searchVenuesForSsr(undefined)` уходил в API
 * без параметра `city`, а не с «Алматы».
 *
 * Значение ОДНО и то же для клиента и сервера: город по умолчанию, пока
 * гость не выбрал свой (см. полный комментарий и историю решения в
 * `city.tsx`, который импортирует его отсюда же).
 */
export const DEFAULT_CITY = "Алматы";
