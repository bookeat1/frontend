/**
 * До восьми гостей: дальше это уже банкет, который заведения принимают
 * отдельно (то же ограничение, что на экране брони в приложении).
 *
 * Живёт отдельным модулем, потому что список нужен ДВУМ несвязанным местам —
 * панели поиска на главной и карточке брони на странице заведения. Импорт
 * константы из модуля компонента утянул бы в бандл страницы заведения саму
 * панель поиска, а копия списка в двух файлах однажды разошлась бы.
 */
export const GUEST_OPTIONS = [1, 2, 3, 4, 5, 6, 7, 8] as const;

/**
 * Сколько гостей выбрано, когда гость ещё ничего не выбирал. Двое — то, что
 * нарисовано в поле «Гости» карточки брони (узел 3525:14744 файла
 * QovvuAoI9YxsLMwWkfgKN8) и в панели поиска.
 */
export const DEFAULT_GUESTS = 2;
